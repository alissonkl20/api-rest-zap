# WhatsApp Automation API

## 🌐 Plataformas de Deploy Gratuito

### 1. Railway (Recomendado)
1. Acesse [railway.app](https://railway.app).
2. Faça login com sua conta do GitHub.
3. Clique em **New Project** → **Deploy from GitHub repo**.
4. Selecione este repositório.
5. O Railway detectará automaticamente o arquivo [`railway.json`](railway.json).

### 2. Render
1. Acesse [render.com](https://render.com).
2. Faça login com sua conta do GitHub.
3. Clique em **New Web Service**.
4. Selecione este repositório.
5. O Render detectará automaticamente o arquivo [`render.yaml`](render.yaml).

### 3. Fly.io
1. Instale o Fly CLI:
   ```sh
   npm install -g flyctl
   ```
2. Faça login no Fly.io:
   ```sh
   flyctl auth login
   ```
3. Inicie o projeto:
   ```sh
   flyctl launch
   ```
4. Faça o deploy:
   ```sh
   flyctl deploy
   ```

## ⚙️ Variáveis de Ambiente

Configure as seguintes variáveis no painel da plataforma escolhida:
- `PORT`: 3000 (auto-configurado).
- `NODE_ENV`: production.

## ⚠️ Observações Importantes

- O **WhatsApp Web** pode desconectar após períodos de inatividade em ambientes serverless.
- Recomenda-se usar **Railway** ou **Render** com planos que mantêm a aplicação sempre ativa.